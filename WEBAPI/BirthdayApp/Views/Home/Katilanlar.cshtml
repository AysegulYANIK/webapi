@model IEnumerable <BirthdayApp.Models.Davetiye>

@if (Model.Count() == 1)
{

    <p>Partiye gelen ilk kişisin.</p>
}

else
{
    <hr />
    <p>
        @foreach (var i in Model)
        {
            <label class="label label-sucess">@i.Ad</label>

        }
    </p>
}